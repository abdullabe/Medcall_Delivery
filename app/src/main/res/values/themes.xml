<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MedcallDelivery" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MedcallDelivery" parent="Base.Theme.MedcallDelivery" />
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/yellow_500</item>
        <item name="colorControlActivated">@color/yellow_500</item>
    </style>
</resources>